<h1>Ajout utilisateur : </h1>

<form method="POST" action="{{form_action}}" id="newUser">
    <input type="hidden" name="id" value="{{formation._id}}" /><br>
    nom : <input type="text" name="nom" value="{{user.nom}}" /><br>
    prenom : <input type="text" name="prenom" value="{{user.prenom}}" /><br>
    login : <input type="text" name="login" value="{{user.login}}" /><br>
    mot de passe : <input type="password" name="password" value="{{user.password}}" /><br>
    <input type ="submit" value="confirme la création" />

    
</form>

<script>
    var use = document.getElementById('newUser');
    use.addEventListener('submit', function(evt) {
        use.action += "/{{user._id}}"
        use.submit();
    });
</script>